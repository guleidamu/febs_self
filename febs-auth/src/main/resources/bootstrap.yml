spring:
  application:
    name: FEBS-Auth
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:3809
      config:
        server-addr: ${nacos.url}:3809
        group: DEFAULT_GROUP
        prefix: febs-auth
        file-extension: yaml