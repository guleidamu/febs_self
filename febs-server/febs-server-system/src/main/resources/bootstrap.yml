spring:
  application:
    name: FEBS-Server-System
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:3809
      config:
        server-addr: ${nacos.url}:3809
        group: DEFAULT_GROUP
        prefix: febs-server-system
        file-extension: yaml